replicaCount: 1

ingress:
  enabled: true
  hosts:
    - host: sparker-api.production.sprk.fi
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: sparker-api-tls
      hosts:
        - sparker-api.production.sprk.fivault:

  mount: production
